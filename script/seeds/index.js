const users = require('./USERS_MOCK_DATA.json')
const employees = require('./EMPLOYEES_MOCK_DATA.json')
const teams = require('./TEAMS_MOCK_DATA.json')

module.exports = {
	users,
	employees,
	teams
}
